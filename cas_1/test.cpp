#include <iostream>

using namespace std;


int main() {
    double broj;

    cin >> broj;

    if(broj > ){
        cout << (double)'A' * broj;
    } else if(broj < ){
        cout << broj - (double)'D' ;
    } else
        cout << "Cestitamo to je trazeni broj";

    return 0;
}